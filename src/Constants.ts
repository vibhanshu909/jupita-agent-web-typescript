const BASE_URL = 'https://api.jupita.io/v1';

export const Constants = {
  name: 'JUPITA',
  url: BASE_URL,
  dumpEndpoint: `${BASE_URL}/dump`,
  feedEndpoint: `${BASE_URL}/feed`,
  ratingEndpoint: `${BASE_URL}/rating`,
};
